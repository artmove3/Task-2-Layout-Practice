mixin button(id, name)
    .button__block 
        a(id=id)&attributes(attributes)= name
mixin buttona(id, name)
    a(id=id)&attributes(attributes)= name
        include ../arrows/forward-white.svg
mixin buttonTransparent(id, name)
    .button__block 
        a(id=id)&attributes(attributes)= name